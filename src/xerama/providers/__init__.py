"""Capability-bearing provider adapters. See ADR-009 and docs/ARCHITECTURE.md
section 6. Only the LLM provider (OpenRouter) is implemented in XER-001;
image/video/voice/lip-sync/storage providers are future adapters behind the
same shape of contract."""
